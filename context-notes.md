# 컨텍스트 노트

## 결정 사항

### 왜 케이스 파일 컨셉인가
덱 22페이지에서 가장 밀도 높은 재료는 스택 목록이 아니라 트러블슈팅 9건이다.
모두 `증상 → 원인 → 조치 → 결과` 구조가 살아 있고, 일부는 정량 지표(216회 → 4회,
8192바이트 버퍼, 14박 2단계 분리)까지 있다. 스택 나열은 어느 주니어 포트폴리오에나
있지만 이 기록은 없다. 그래서 프로젝트마다 케이스 블록을 두고 4단 구조를 그대로 노출했다.

### 디자인 토큰
- 바탕 `#E4E3DD` (putty). 크림(#F4F1EA)보다 회색기가 강해 산업적 인상.
- 잉크 `#16171A`, 보조 `#45474C`, 흐림 `#85878C`, 괘선 `#C7C6BF`
- 액센트 **코발트 `#1B3AE8` 단 하나**. 링크, 케이스 결과, 현재 섹션 표시에만 사용.
  액센트를 하나로 묶어야 케이스 결과가 페이지에서 제일 먼저 눈에 들어온다.
- 다크 배경 + 네온 액센트, 크림 + 세리프 + 테라코타 조합은 의도적으로 회피.

### 타이포
- Display: **Archivo** 가변폰트, `wdth` 축을 108~120으로 확장. 이름/프로젝트명 전용.
- Body: **Pretendard** — 한글 본문 비중이 압도적이라 선택.
- Utility: **IBM Plex Mono** — 메타데이터, 라벨, 스택 태그.
- 프로젝트명이 전부 라틴(WAAIT, SBLIM, Trip_Helper, TheMoa)이라 라틴 디스플레이 페이스를
  붙여도 한글 본문과 충돌하지 않는다.

### 레이아웃
- 좌측 고정 스파인(56px). 세로 회전 모노 텍스트 + 현재 섹션 번호. 900px 미만에서 숨김.
- 본문은 `라벨(모노, 좁은 좌측 컬럼) / 내용(넓은 우측 컬럼)` 비대칭 그리드.
- 프로젝트 순서는 시간순 오름차순. JSP+MyBatis → Spring AI+RAG 로 이어지는 성장 곡선이
  그 자체로 정보라서 번호를 붙였다(순서가 의미를 갖는 경우에만 넘버링).

### 스택
빌드 도구 없음. `index.html` + `styles.css` + `script.js` 3파일.
더블클릭으로 바로 열리고, 어디에 올려도 정적 호스팅이 된다. 포트폴리오 규모에 번들러는 과함.

### 모션
- 로드 시 마스트헤드 1회 시퀀스, 스크롤 시 섹션 상승 리빌.
- 숫자 카운트업은 넣지 않았다. AI 생성 티가 가장 많이 나는 장치라서.
- `prefers-reduced-motion: reduce` 에서 transform/opacity 전환 전부 무효화.

## 덱에서 발견한 불일치 (원문 유지, 여기에만 기록)

1. **SBLIM 인원** — 헤더는 "팀 프로젝트 (2인)", 본문은 "3인 팀에서 팀장으로". 헤더 값(2인) 채택.
2. **SBLIM 기간** — "2024.06 – 2024.08" 로 적혀 있으나 리포지토리명이 `kosta308_mini_project`.
   KOSTA308 과정은 2026.02–2026.07 이므로 2026년일 가능성이 높음. **원문 그대로 두었음.**
3. **WAAIT 리포지토리** — `erp-project-waait-2025` 인데 기간은 2024.06–2024.08. 원문 유지.
4. **Trip_Helper URL** — 덱 원문 `github.com/K-SOLMIN/K-SOLMIN/Trip_Helper/tree/main` 은
   경로가 중복돼 열리지 않음. `https://github.com/K-SOLMIN/Trip_Helper` 로 보정. **확인 필요.**

## 교정한 오타 (포트폴리오라 그대로 두면 감점이라 수정)
- "기획부터 배포까지" 계열 문장 다듬기
- `밑` → `및` (2곳: "교구 반납/예약 밑 교구관리", "HTTPS 처리 밑 HTTP 리다이렉트")
- `관관명소` → `관광명소`
- `private 영엑` → `private 영역`
- `jjoin fetch` → `join fetch`
- `MySql` → `MySQL`, `REACT` → `React`, `Jquery` → `jQuery`, `OAUTH2` → `OAuth 2.0`
- "3인 팀에서 팀장으로 기획부터 배포까지 주도했습니다" (SBLIM) → 2인으로 정정

---

# 2차 작업 — 이미지

원본은 `C:\project_output`. GitHub Pages 배포를 전제로 **원본 경로를 참조하지 않고
`C:\portfolio\assets\` 로 복사**했다. 저장소만 올리면 이미지가 따라가야 하기 때문이다.

## 변환
`System.Drawing`(PowerShell)으로 가로 최대 1800px(ERD만 2400px) 리사이즈 후 JPEG q86.
**19.7MB → 3.4MB (24장).** WebP가 더 좋지만 Pillow 스크립트 실행이 권한 정책에 막혀
Windows 기본 이미지 API로 처리했다. 다시 하려면 `Pillow` 로 WebP q82 변환이 더 작다.

전부 `loading="lazy"` + `decoding="async"` + `width`/`height` 명시라 초기 로드에는
거의 영향이 없고 레이아웃 흔들림(CLS)도 없다.

## 캡션 근거
파일명만으로 내용을 알 수 없는 12장은 직접 열어 확인하고 캡션을 달았다.
특히 아래 넷은 이름이 서로 헷갈렸다.

| 파일 | 실제 내용 |
|---|---|
| `ai_schedule_ready.png` | 맞춤 입력 폼 (STEP 01 여행지 선택) |
| `ai_generation_due.png` | 생성 중 대기 화면 (진행률 6%, 4단계) |
| `ai_generation_result.png` | 생성 완료 결과 (일차별 일정 + Maps 동선) |
| `ai_generation_while_schedule.png` | 여행 중 화면 (D01) |

`일정중.png` 도 여행 중 화면(D02)이라 둘을 D01·D02로 나눠 캡션했다.

## 라이트박스
스크린샷이 2단 그리드에서 글씨를 읽을 수 없어 확대 보기를 넣었다.
네이티브 `<dialog>` + `showModal()` 을 썼다 — Esc 닫기, 포커스 관리, `::backdrop` 이
브라우저 기본 동작으로 따라와 직접 구현할 게 없다.
`close` 이벤트로 이미지 src를 비우는 정리 코드를 넣었다가 뺐다. 검증 환경에서 이벤트가
발화하지 않아 동작을 확인할 수 없었고, 이미지 한 장 캐시는 신경 쓸 크기가 아니다.

## 제외한 것
- **`apricot-shoppingmall-project` 3장** (장바구니, 주문서, 배송지 입력).
  Canva 덱에 이 프로젝트 설명이 아예 없다. 사진만 보고 프로젝트 섹션을 지어내는 건
  포트폴리오에서 할 일이 아니라 뺐다. 넣으려면 개요·담당·스택 텍스트가 필요하다.

## 포함하되 짚어 둘 것
- **`themoa/notion-env.jpg`** — Notion의 Env 페이지 원문이라 `MYSQL_USERNAME=root`,
  `MYSQL_PASSWORD=1111` 등이 그대로 보인다. localhost 로컬 개발값이라 실제 악용
  위험은 없다고 사용자가 판단해 포함했다. 다만 이 이미지가 붙은 항목의 문구가
  "민감정보가 Git에 노출되지 않도록"이라 읽는 사람에 따라 상반돼 보일 수 있다.
- **Notion 스크린샷 3장에 팀원 실명(박영균)이 보인다.** 공개 배포 전 확인 필요.
